line-length = 100
target-version = "py311"

[lint]
select = ["E", "F", "W", "I"]
ignore = ["E402"]  # module-level imports after sys.path edits are intentional

[lint.per-file-ignores]
"tests/*" = ["E501"]
